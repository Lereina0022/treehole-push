name: Treehole Push

on:
  schedule:
    - cron: "0 */4 * * *"
  workflow_dispatch:

jobs:
  run-task:
    runs-on: ubuntu-latest

    env:
      TREEHOLE_AUTHORIZATION: ${{ secrets.TREEHOLE_AUTHORIZATION }}
      TREEHOLE_COOKIE: ${{ secrets.TREEHOLE_COOKIE }}
      TREEHOLE_X_XSRF_TOKEN: ${{ secrets.TREEHOLE_X_XSRF_TOKEN }}
      TREEHOLE_UUID: ${{ secrets.TREEHOLE_UUID }}
      SERVERCHAN_SENDKEY: ${{ secrets.SERVERCHAN_SENDKEY }}
      DB_PATH: data/treehole.db

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Ensure data directory exists
        run: mkdir -p data

      - name: Run script
        run: python main.py
